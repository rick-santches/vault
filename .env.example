# PostgreSQL connection string.
# Local dev: run `docker compose up -d` (see docker-compose.yml) or point
# at any Postgres. Production: a managed Postgres like Neon.
DATABASE_URL="postgresql://vault:vault@localhost:5432/vault?schema=public"

# Session signing secret — generate a real one:  openssl rand -base64 32
JWT_SECRET=""
